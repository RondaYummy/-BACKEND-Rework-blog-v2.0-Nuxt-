const Posts = require('./Posts');
const User = require('./User');
const UserToken = require('./UserToken');
const Comments = require('./Comments');
const RequestFriends = require('./FriendsRequest');

module.exports = {
  Posts,
  User,
  UserToken,
  Comments,
  RequestFriends
};
